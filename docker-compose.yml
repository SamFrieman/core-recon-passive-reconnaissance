version: '3.8'

services:
  api:
    build: .
    command: uvicorn backend.main:app --host 0.0.0.0 --port 8000
    volumes:
      - .:/app
      - corerecon_data:/app/data
    ports:
      - "8000:8000"
    environment:
      - DB_PATH=/app/data/recon_history.db

volumes:
  corerecon_data:
