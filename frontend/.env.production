# This tells the frontend to use relative /api paths,
# which Vercel will proxy to your Render backend via vercel.json rewrites.
# You do NOT need to change this file - Vercel handles the routing.
VITE_API_URL=
