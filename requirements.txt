# CoreRecon v2.0 — Backend Dependencies
# All v1 dependencies preserved. New additions marked with # NEW

# Web framework
fastapi
uvicorn[standard]

# DNS resolution
dnspython

# WHOIS
python-whois

# HTTP client
requests

# Technology fingerprinting (preserved from v1)
python-wappalyzer

# PDF generation (preserved from v1)
fpdf2

# Certificate parsing (preserved from v1)
cryptography

# TLD extraction for input normalization # NEW
tldextract

# Rate limiting # NEW
slowapi

# Suppress SSL warnings in passive header checks
urllib3
